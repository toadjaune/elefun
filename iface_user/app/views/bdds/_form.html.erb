<%= form_for(@bdd) do |f| %>
  <% if @bdd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bdd.errors.count, "error") %> prohibited this bdd from being saved:</h2>

      <ul>
      <% @bdd.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
